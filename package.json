{
  "name": "http-mcp",
  "version": "0.1.0",
  "description": "MCP stdio server for HTTP requests — curl-equivalent functionality via Node's undici/fetch with structured responses",
  "type": "module",
  "main": "dist/index.js",
  "bin": { "http-mcp": "dist/index.js" },
  "files": ["dist", "README.md", "LICENSE"],
  "scripts": { "build": "tsc", "start": "node dist/index.js" },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.1",
    "zod": "^3.23.8",
    "undici": "^7.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.7.0"
  }
}
